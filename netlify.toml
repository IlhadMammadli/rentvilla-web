[build]
  command = "npx prisma generate && npx prisma db push && npm run build"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"

# Next.js is handled by Netlify's OpenNext integration automatically.
